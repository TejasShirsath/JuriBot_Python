# Example environment variables (not used by default, but available for advanced configuration)
# Copy this file to .env if you want to use environment variables instead of secrets.toml

# Google Gemini API Key
GEMINI_API_KEY=your_api_key

# Database path (optional, defaults to juribot.db)
# DB_PATH=juribot.db

# Tesseract path (optional, if not in PATH)
# TESSERACT_PATH=C:\Program Files\Tesseract-OCR\tesseract.exe

# Poppler path (optional, if not in PATH)
# POPPLER_PATH=C:\poppler\Library\bin

# Application settings
# MAX_UPLOAD_SIZE_MB=10
# DEFAULT_LANGUAGE=English
